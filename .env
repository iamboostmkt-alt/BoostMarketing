DATABASE_URL=file:/home/z/my-project/db/custom.db
NEXTAUTH_SECRET=boostmarketing-secret-key-2024-secure
# NEXTAUTH_URL is NOT set — trustHost:true in NextAuth config
# lets NextAuth detect the host from the request headers.
# This is REQUIRED for preview/proxied environments where
# the host header doesn't match localhost.
